# Database connection string (SQLite for local dev)
DATABASE_URL=sqlite+aiosqlite:///data/bonus_tracker.db

# Allowed CORS origins (JSON array, needed for local dev without proxy)
CORS_ORIGINS=["http://localhost:5173"]

# Default bonus rate for new projects (2%)
DEFAULT_BONUS_RATE=0.02
